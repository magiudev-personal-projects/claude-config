Always give short replies (max 3 lines). Don't over-explain. Get to the point and communicate in simple terms.

# Git commits

The user manages git staging themselves. When asked to commit, never run `git add` or stage files — only commit what is already staged. If nothing is staged, say so instead of staging anything.